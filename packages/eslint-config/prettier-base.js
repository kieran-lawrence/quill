/**
 * @see https://prettier.io/docs/configuration
 * @type {import("prettier").Config}
 */
const config = {
    singleQuote: true,
    useTabs: false,
    semi: false,
    trailingComma: 'all',
    tabWidth: 4,
}

export default config
